import React, { Component } from 'react';
import { API_ROOT, HEADERS } from '../constraints/Constraints'

class Home extends Component {
  constructor(props) {
    super(props)
    this.state = {
      routines =[]
    }
  }
}